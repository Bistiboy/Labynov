public class NewBehaviourScript : MonoBehaviour
{

    [SerializedField] string signe;
    [SerializedField] string nom;
    [SerializedField] int positionX;
    [SerializedField] int positionY;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
